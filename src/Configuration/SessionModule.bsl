
Процедура УстановкаПараметровСеанса(ТребуемыеПараметры)
	
	Имя = ИмяПользователя();
	
	ФизЛицо = Справочники.ФизическиеЛица.НайтиПоКоду(Имя);
	
	Если ФизЛицо.Пустая() Тогда
		НовоеФизЛицо = Справочники.ФизическиеЛица.СоздатьЭлемент();
		НовоеФизЛицо.Наименование = Имя;
		НовоеФизЛицо.Код = Имя;
		НовоеФизЛицо.Записать();
		
		ФизЛицо = НовоеФизЛицо;
	КонецЕсли;
	
	ПараметрыСеанса.ТекущийПользователь = ФизЛицо;
	
КонецПроцедуры
